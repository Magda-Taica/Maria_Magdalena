@page
@model Maria_Magdalena.Pages.Angajati.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Angajat[0].numeangajat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Angajat[0].post)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Angajat[0].Manager)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Angajat[0].Departament)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Angajat) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.numeangajat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.post)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Manager.numemanager)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Departament.dept)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
